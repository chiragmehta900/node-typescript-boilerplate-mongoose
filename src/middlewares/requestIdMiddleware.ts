// import { Request, Response, NextFunction } from 'express';
// import { v4 } from 'uuid';

// const requestIdMiddleware = async function (
//     req: Request,
//     res: Response,
//     next: NextFunction
// ) {
//     const requestId = v4();
//     req.headers['X-Request-Id'] = requestId;
//     res.setHeader('X-Request-Id', requestId);
//     next();
// };

// export default requestIdMiddleware;
